# Skill: commit

Stage all changes, write a concise Hebrew commit message describing what changed, commit and push to main.

## Steps
1. Run `git status` to see what changed
2. Run `git diff` to understand the changes
3. Stage relevant files (avoid secrets, large binaries)
4. Write a commit message in Hebrew (one line, imperative form)
5. Commit and push to origin main
